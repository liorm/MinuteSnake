{
  "name": "minute-snake",
  "version": "1.0.0",
  "description": "Modern Snake game implementation",
  "type": "module",
  "scripts": {
    "clean": "rimraf dist",
    "prebuild": "npm run clean",
    "build": "node build.js",
    "serve": "http-server ."
  },
  "devDependencies": {
    "esbuild": "^0.20.0",
    "http-server": "^14.1.1",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "seedrandom": "^3.0.5"
  }
}
